/*
   Incluya en este fichero todas las definiciones que pueden
   necesitar compartir todos los módulos (editor, subscriptor y
   proceso intermediario), si es que las hubiera.
*/


typedef struct mensaje{
  int peticion;
  char tema[50];
  char evento[50];
  char host[50];
}msg_t;

/*
  Esta función devuelve un mensaje por el socket de la conexión con el cliente,
  guardando dicho mensaje en una structure. Si hay exito devuelve 0.
  Si no, devuelve -1.
*/
int recibirMensaje(int socket, struct mensaje *mensaje);

/*
Esta función envía un mensaje a traves del socket de la conexión con el cliente,
especificando el mensaje a enviar. Si hay exito devuelve 0. Si no, devuelve
-1.
*/
int enviarMensaje(int socket, struct mensaje *mensaje);



